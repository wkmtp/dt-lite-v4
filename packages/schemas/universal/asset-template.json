{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dt-lite.schemas/universal/asset-template.json",
  "title": "AssetTemplate",
  "description": "Universal AssetTemplate Contract v1.0 — FROZEN. Template for instantiating Assets with parameter binding.",
  "type": "object",
  "required": ["id", "code", "name", "version", "asset_schema", "point_templates", "capability_templates", "relationship_templates", "instantiation_params_schema"],
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "code": {
      "type": "string",
      "minLength": 1,
      "description": "Unique template code"
    },
    "name": {
      "type": "string",
      "minLength": 1
    },
    "version": {
      "type": "string",
      "description": "SemVer version (e.g., 1.0.0)",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "asset_schema": {
      "type": "object",
      "description": "JSON Schema reference to Asset.json — defines valid Asset structure for this template"
    },
    "point_templates": {
      "type": "array",
      "description": "Point definitions generated by this template",
      "items": {
        "type": "object",
        "required": ["code", "semantic_type", "unit", "aggregation"]
      }
    },
    "capability_templates": {
      "type": "array",
      "description": "Capability definitions provided by this template",
      "items": {
        "type": "object",
        "required": ["code", "safety_level"]
      }
    },
    "relationship_templates": {
      "type": "array",
      "description": "Relationship definitions provided by this template",
      "items": {
        "type": "object",
        "required": ["type", "direction"]
      }
    },
    "instantiation_params_schema": {
      "type": "object",
      "description": "JSON Schema for template instantiation parameters"
    }
  },
  "additionalProperties": false
}
